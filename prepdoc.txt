WRITE PSUDO CODE HERE

MEDIA CLASS
	public
		Media()
		char* getYear()
		int getTitle()
	private
		int year
		char* title

GAME CLASS
	public 
		Game()
		Game(title, year, publisher, rating)
		char* getPublisher()
		float getRating()
	private
		char* publisher
		float rating



MUSIC CLASS

MOVIE CLASS

MAIN FILE

vector<Media*> mediaVect;

func delete(title, year)

	for each media in mediaVect
		if media.title = title or if media.year = year
			ask user if want to delete
				if answer = y
					delete that shit

func search(title, year)

	for each media in media vector 
		if media.title = title or if media.year = year
			media.printdetails()

func add(mediaType)

	if mediatype = "game"
		char[80] title
		getline(title)
		int year
		cin >> year
		char[80] pub
		getline(pub)
		float rating
		cin >> rating	
		Game* newgame = Game(title, year, pub, rating)
		vector.append(newGame)


	repeat for each mediatype
